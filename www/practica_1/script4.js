// Declaramos as constantes e as variables
const PI = 3.1416
let radio = 5

// Calculamos o Área da circunferencia
let area = PI * radio * radio

// Declaramos variable de cor
let corCirculo = "vermello"

//reasignamos o valor da cor
corCirculo = "azul"

//Imprimimos por pantalla
console.log("Área: " + area + ", Cor: " + corCirculo)